language: python
sudo: required
python: "3.6"
install:
  - pip install pipenv
  - pipenv install
  - cd scripts
  - chmod +x downloadTestData.sh
  - sh downloadTestData.sh
  - cd ..
script:
  - coverage run test.py all
after_script:
  - coverage report